# Core Freeze Guard — build fixture fix

- Replaces the invalid fixture role `route` with the valid inventory role `api-route`.
- Changes only `coreFreezeGuard.smoke.ts`.
- Runtime selection and authorization logic is unchanged.
